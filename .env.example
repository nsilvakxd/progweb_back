# Ambiente da aplicação
# development - usa DATABASE_URL do .env (banco local)
# production - usa URL hardcoded do banco de produção no Render
ENVIRONMENT="development"

# Chave secreta para assinar os tokens JWT (MUDE PARA UMA CHAVE SEGURA EM PRODUÇÃO)
SECRET_KEY="sua-chave-secreta-aqui"

# String de conexão com o banco PostgreSQL (usado apenas em development)
# Formato: postgresql://usuario:senha@host/nome_banco
DATABASE_URL="postgresql://postgres:password@localhost/progwebIII"
